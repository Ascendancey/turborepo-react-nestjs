{
  "name": "turborepo-react-nestjs",
  "private": true,
  "scripts": {
    "start": "turbo run dev",
    "front": "turbo run dev --filter=frontend",
    "back": "turbo run dev --filter=backend",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "clean": "rm -rf node_modules/ apps/*/node_modules"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "prettier": "^3.4.1",
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
